measure = str(input("F or C: "))
temperature = float(input("Input temperature: "))

if measure.lower() in ['f']:
    print((temperature - 32) * 5 / 9)

elif measure.lower() in ['c']:
    print((temperature * 9 / 5) + 32)
